server:
  port: 8080

# Todo: 要改成真正的库名和密码

mysql:
  dsn: "user:password@tcp(127.0.0.1:3306)/ai_task?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  addr: "127.0.0.1:6379"
  password: ""
  db: 0

jwt:
  secret: "dev-secret"
  expire_seconds: 7200

rate_limit:
  ip_limit_per_minute: 30
  user_limit_per_minute: 30

task:
  redis_status_ttl_seconds: 1800

mq:
  url: "amqp://user:password@127.0.0.1:5672/ai_task"
  exchange: "ai.task.exchange"
  queue: "ai.task.queue"
  routing_key: "ai.task.create"
  max_retry: 3
